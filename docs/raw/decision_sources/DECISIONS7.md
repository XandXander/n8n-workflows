DECISIONS.mdProject Decision LogDecision: Выбор официального Public API v1 n8n вместо внутреннего REST интерфейсаDateИюль 2026 годаContextИсходный скрипт автоматизации деплоя n8n_git_manager отправлял запросы на эндпоинты вида /rest/workflows, которые скопированы из веб-интерфейса браузера. Запросы завершались ошибкой авторизации Unauthorized, так как приватный эндпоинт требовал куки сессии браузера, а не переданный токен авторизации.DecisionБыло принято решение полностью переписать логику сетевых запросов в Python-скрипте на использование официального публичного API n8n (Public API v1). Маршруты изменены с приватных на публичные.Alternatives ConsideredЭмуляция браузерной сессии с сохранением сессионных кук (Session Cookies) через скрипт автоматизации.Использование официального Public API v1 n8n.Why This Decision Was MadeОфициальный Public API v1 нативно поддерживает авторизацию через заголовок X-N8N-API-KEY.Публичный программный интерфейс является стабильным и документированным, в отличие от скрытых внутренних путей UI.Trade-offsПлюсы:Стабильная и безопасная авторизация по API-ключу.Исключение рисков поломки скрипта при обновлении дизайна интерфейса n8n.Минусы:Публичный API требует явного включения в конфигурации инстанса (N8N_API_ENABLED=true), что накладывает требования к настройке серверного окружения.ConsequencesПайплайн интеграции Open WebUI и n8n успешно прошел авторизацию на сервере. Ошибка Unauthorized полностью устранена.Current StatusAcceptedDecision: Смена HTTP-метода PUT на PATCH для обновления воркфлоуDateИюль 2026 годаContextПосле перевода скрипта автоматизации на Public API v1 n8n, попытки отправить изменения существующего воркфлоу через HTTP-метод PUT приводили к ошибкам сервера, так как структура путей публичного API не поддерживала тотальную перезапись сущностей через данный метод на эндпоинте конкретного ID.DecisionПринято решение изменить HTTP-метод запроса в методе update_n8n_workflow с PUT на PATCH. Данное решение синхронизировано и зафиксировано в технической документации инфраструктуры ENVIRONMENT.md.Alternatives ConsideredИспользование метода PUT (отвергнуто из-за несовместимости с Public API n8n).Использование метода PATCH.Why This Decision Was MadeСпецификация n8n Public API v1 жестко регламентирует использование метода PATCH для частичного или полного обновления схемы воркфлоу по его идентификатору (/api/v1/workflows/{id}).Trade-offsПлюсы:Полное соответствие REST-стандартам платформы n8n.Корректное применение изменений без сбоев на стороне сервера.Минусы:Отсутствуют.ConsequencesОбновление схем воркфлоу из репозитория на стороне сервера Beget через вызовы из Open WebUI стало работать корректно и без ошибок.Current StatusAcceptedDecision: Доступ к флагманским LLM моделям через Google AI Studio вместо Google One AI ProDateИюль 2026 годаContextПри попытке войти в интерфейс Gemini Advanced по ссылкам платной потребительской подписки Google One AI Pro из-под рабочего аккаунта Google Workspace, система выдавала блокирующее предупреждение с требованием переключиться на личный аккаунт @gmail.com. Использовать подписку Google One в корпоративном профиле было невозможно из-за ограничений Google Workspace.DecisionПринято решение отказаться от интерфейсов Google One/Gemini Advanced в рамках рабочего профиля и использовать платформу для разработчиков Google AI Studio. В качестве основной рабочей Pro-модели выбран алиас gemini-pro-latest (указывающий на актуальную версию Gemini Pro с глубоким рассуждением).Alternatives ConsideredПокупка официальной корпоративной лицензии Gemini Business / Enterprise через администратора организации.Переключение на личный аккаунт @gmail.com и полный перенос рабочих процессов туда.Использование Google AI Studio под существующим рабочим аккаунтом.Why This Decision Was MadeGoogle AI Studio беспрепятственно разрешает авторизацию с рабочих аккаунтов Google Workspace (если это не запрещено локальной политикой администратора).Инструмент предоставляет прямой доступ к флагманской Pro-модели, гибкую настройку системных инструкций и бесплатную генерацию API-ключей разработчика для интеграций.Trade-offsПлюсы:Доступ к топовой модели ИИ сохранен внутри рабочего аккаунта без финансовых затрат.Появился удобный интерфейс для генерации API-ключей, необходимых для скрипта Pipe в Open WebUI.Минусы:Интерфейс AI Studio ориентирован на разработчиков и лишен некоторых потребительских функций (например, прямой интеграции с личным Workspace-диском, как в Gemini Advanced).ConsequencesПользователь получил стабильный доступ к флагманскому ИИ. Модель была успешно переключена в режим глубокого анализа (Thinking level: High) для минимизации логических ошибок.Current StatusAcceptedDecision: Использование DeepSeek V4 Pro в качестве основного ядра для работы со схемамиDateЕсли дата неизвестнаContextВ процессе проектирования среды для генерации и редактирования воркфлоу встал вопрос о выборе целевой языковой модели, которая будет встроена в Open WebUI в качестве экспертного агента. Требовалась модель, способная без ошибок выдавать сложные, многоуровневые JSON-структуры связей n8n.DecisionВ качестве главного ИИ-редактора и аналитика воркфлоу выбрана модель DeepSeek V4 Pro (флагманская модель с поддержкой глубокого логического рассуждения Reasoning).Alternatives ConsideredGemini 3.1 Pro (оставлена для мультимодальных OSINT-задач, анализа медиа, документов и глубокого интернет-поиска).DeepSeek V4 Flash (оставлена для дешевой и быстрой рутинной автоматизации через фоновые API-скрипты).DeepSeek V4 Pro.Why This Decision Was MadeМодели семейства DeepSeek демонстрируют высочайшее качество генерации чистого кода и строгого следования синтаксису разметки (Markdown/JSON) в рамках инженерных задач.Проекту требовался глубокий пошаговый анализ логики узлов (Chain-of-Thought), который в полной мере обеспечивает Pro-версия DeepSeek.Trade-offsПлюсы:Максимально точная генерация JSON-структур связей и внутренних JS-скриптов.Строгое соблюдение формата управляющих команд для пайплайна.Минусы:Отсутствие нативной мультимодальности (модель работает только с текстом и не может напрямую анализировать изображения интерфейсов без перевода в текстовое описание).ConsequencesАгент Open WebUI переведен под управление DeepSeek V4 Pro, системные промты и файлы базы знаний (n8n_schema.md, node_templates.md) оптимизированы под особенности токенизации и генерации данной модели.Current StatusAcceptedDecision: Принудительное удаление поля ID из объектов массива NodesDateИюль 2026 годаContextПри написании файлов базы знаний (node_templates.md) и системных промтов для Open WebUI ИИ-модели по умолчанию добавляли во фрагменты JSON-кода узлов поле "id": "uuid". Это полностью ломало деплой, так как Public API n8n при PATCH-обновлении запрещает ручную передачу идентификаторов нод, возвращая ошибку 400 Bad Request.DecisionПринято архитектурное решение полностью исключить поле id из всех спецификаций, описывающих формат отдельного узла (Node) внутри массива nodes. В системный промт Open WebUI внедрено жесткое, выделенное капслоком правило-запрет. Узлы связываются в карте связей исключительно по уникальному текстовому полю name.Alternatives ConsideredГенерация валидных случайных UUID на стороне LLM (отвергнуто, так как API n8n все равно их отклоняет).Полный запрет на генерацию поля id на уровне узлов и идентификация только по name.Why This Decision Was MadeТехническое требование серверного валидатора и Public API n8n. Сервер n8n обязан самостоятельно назначать UUID внутренним узлам при накатывании патча схемы.Trade-offsПлюсы:Полное устранение ошибок 400 Bad Request при импорте сгенерированных схем на сервер.Код стал более лаконичным и читаемым для модели.Минусы:Требует от инженера строгого контроля за тем, чтобы имена узлов (name) внутри одного воркфлоу никогда не дублировались, иначе карта связей нарушится.ConsequencesВсе шаблоны нод (HTTP Request, Telegram, Google Sheets) приведены к валидному виду. Ошибки импорта из-за дублирования или некорректного формата ID узлов прекратились.Current StatusAcceptedDecision: Внедрение архитектурных механизмов контроля контекста (Метод «Сброса балласта»)DateИюль 2026 годаContextВ процессе ведения длинных диалогов в Open WebUI модель DeepSeek со временем начинала «тупеть»: теряла нить системных ограничений, генерировала мусорные поля из черного списка, обрезала код JSON многоточиями ... и выдавала синтаксические ошибки из-за перегрузки контекстного окна.DecisionПринято решение внедрить в системный промт модели жесткий архитектурный паттерн управления контекстом — Метод «Сброса балласта» с командным триггером «сброс памяти» или «очисти контекст». Также зафиксировано требование постоянного использования режима глубокого рассуждения (Thinking level: High).Alternatives ConsideredРегулярное ручное открытие нового чистого окна чата пользователем (неудобно, так как теряются текущие договоренности по архитектуре проекта).Использование скриптов автоматической суммаризации на уровне Python-кода Pipe (отложено из-за усложнения кода).Программное внедрение триггера «сброса балласта» прямо в системные инструкции ИИ в Open WebUI.Why This Decision Was MadeМетод позволяет очистить «оперативную память» модели от накопленного мусора (старых ошибок, ложных вариантов кода, флуда), заставляя ИИ сделать лаконичную выжимку текущего статуса архитектуры и продолжить работу в режиме максимальной концентрации.Trade-offsПлюсы:Восстановление высокого качества и точности ответов модели в рамках одной длинной сессии.Исключение генерации битого JSON с многоточиями.Минусы:Пользователь должен самостоятельно отслеживать момент деградации модели и отправлять триггерную команду в чат.ConsequencesПроблема логического угасания ИИ к концу длинных сессий автоматизации была успешно локализована и переведена под ручной контроль через триггерные фразы.Current StatusAcceptedMajor Architectural DecisionsПайплайн «Инструмент на базе текста (Text-Driven Pipeline)»: Вся автоматизация разработки воркфлоу завязана на текстовый интерфейс Open WebUI. Модель генерирует полный JSON, который размечается тегами json , а в самом конце ответа выводит строгую изолированную строку-команду (например, обновить имя_файла.json или создать имя_файла.json). Локальный скрипт Pipe перехватывает эту строку, отправляет JSON на валидацию, осуществляет деплой на сервер Beget через API и автоматически фиксирует изменения в Git-репозитории.Двухуровневое разделение ролей ИИ:DeepSeek V4 Pro назначен главным архитектором и редактором JSON-схем, так как обладает наивысшей точностью в генерации структурированного кода и строго следует правилам черного списка полей.Gemini Pro (latest) через Google AI Studio назначена ядром для мультимодальных OSINT-задач, глубокого интернет-поиска (Deep Research) и анализа неструктурированных документов/медиафайлов.Technology Choicesn8n (Self-hosted на Beget): Выбран в качестве основного движка автоматизации бизнес-процессов. Позволяет разворачивать кастомные интеграции без ограничений на количество шагов выполнения, в отличие от облачных платформ.Docker / Docker Compose: Используется на сервере Ubuntu 24.04 LTS для развертывания n8n в связке с PostgreSQL (с расширением pgvector под будущие RAG-задачи), Redis (для бэкенда очередей задач) и Traefik (в качестве обратного прокси с автоматическим HTTPS от Let's Encrypt). Обеспечивает легкую масштабируемость и изоляцию сервисов.Google AI Studio: Выбрана как точка доступа к API моделей Google (Gemini) для обхода ограничений корпоративных подписок Google Workspace на персональные тарифные планы Google One.Tavily Search: Интегрирован как специализированная поисковая система для ИИ-компонентов воркфлоу, обеспечивающая оптимизированную выдачу результатов без лишнего веб-мусора.n8n-workflow-validator: Утилита установлена глобально в локальном окружении Windows 10. Выбрана в качестве обязательного промежуточного фильтра, который проверяет сгенерированный ИИ код на структурные ошибки до того, как запрос уйдет на боевой сервер.Rejected ApproachesИспользование метода PUT для деплоя через Public API: Данный подход был полностью отвергнут, так как официальный эндпоинт n8n /api/v1/workflows/{id} архитектурно спроектирован под метод PATCH. Попытки использовать PUT вызывали сбои и ошибки серверов.Ручное прописывание UUID (id) внутри нод: Подход отвергнут, так как ручные ID узлов ломают логику импорта воркфлоу через Public API n8n. Система жестко переведена на беспроигрышную связку по именам (name), а генерация ID делегирована серверу n8n.Использование потребительской подписки Gemini Advanced внутри рабочего Workspace-аккаунта: Полностью отвергнуто из-за непреодолимых лицензионных ограничений экосистемы Google. Вся разработка перенесена в бесплатную среду Google AI Studio.Cost vs Quality DecisionsИспользование бесплатной среды Google AI Studio вместо платных подписок Google One AI Pro: Решение продиктовано не экономией, а технической невозможностью подружить Workspace-аккаунт с персональным тарифом. В итоге это позволило получить доступ к оригинальным Pro-моделям и API-ключам без финансовых затрат.Разделение задач между DeepSeek V4 Pro и Flash: Для ответственных задач проектирования воркфлоу n8n выбрана более дорогая/медленная, но качественная модель Pro-уровня (с глубоким мышлением), в то время как дешевая и быстрая модель Flash зарезервирована под массовую обработку рутинных API-запросов и скриптов. Это обеспечивает оптимальный баланс между бюджетом и качеством архитектуры.Lessons LearnedИИ-модели требуют анти-шаблонов: Простого указания «не делай чего-то» в промте часто недостаточно для LLM (особенно в длинных сессиях). Модели гораздо лучше понимают ограничения, когда в их базу знаний (node_templates.md) загружен явный пример JSON, размеченный комментариями «❌ ЗАПРЕЩЕНО».Внутренние и публичные стандарты платформ часто расходятся: Разработка наглядно показала, что пути и методы, используемые веб-интерфейсом n8n для отрисовки холста в браузере (/rest/), кардинально отличаются от логики работы официального публичного API для разработчиков (/api/v1/). При автоматизации деплоя всегда следует опираться исключительно на публичные задокументированные API.Decision DebtБезопасность API-ключей в коде (Хардкод): В текущей реализации Python-скрипта Pipe приватный ключ авторизации n8n (X-N8N-API-KEY) жестко прописан внутри тела методов. Это временное решение, требующее незамедлительного переноса токена в безопасную секцию настроек Valves интерфейса Open WebUI.Отсутствие гибких шаблонов связей для AI-компонентов: В документе спецификации структуры n8n_schema.md детально описан только базовый тип выхода связей (main). Шаблоны для сложных AI-портов LangChain (таких как ai_tool, ai_languageModel, ai_memory), активно используемых в OSINT-стеке, еще не зафиксированы в базе знаний, что может привести к ошибкам генерации ИИ-агентов в будущем.Отсутствие валидации синтаксиса встроенного JS-кода: Локальный n8n-workflow-validator проверяет только верхнеуровневую структуру JSON-схемы воркфлоу, но не заглядывает внутрь параметра jsCode ноды Code. Если модель допустит ошибку в JavaScript-коде, битый сценарий будет успешно задеплоен на сервер и упадет только во время реального выполнения (Runtime). Требуется разработка кастомного навыка (Skill) для валидации JS-кода перед отправкой.